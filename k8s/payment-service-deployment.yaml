apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-servcie-app
spec:
  selector:
    matchLabels:
      app: payment-servcie-app
  template:
    metadata:
      labels:
        app: payment-servcie-app
    spec:
      containers:
      - name: payment-servcie-app
        image: purnachanderpathi/paymentservice
        ports:
        - containerPort: 8081
        env:
          - name: EUREKA_SERVER_ADDRESS
            valueFrom:
              configMapKeyRef:
                key: eureka_service_address
                name: eureka-cm
          - name: CONFIG_SERVER_URL
            valueFrom:
              configMapKeyRef:
                key: config_url
                name: config-cm
          - name: DB_HOST
            valueFrom:
              configMapKeyRef:
                key: hostname
                name: mysql-cm

---
apiVersion: v1
kind: Service
metadata:
  name: payment-servcie-svc
spec:
  selector:
    app: payment-servcie-app
  ports:
  - port: 80
    targetPort: 8081

